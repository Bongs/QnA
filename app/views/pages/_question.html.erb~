<div class="row">
	<div class="span9 qna-que">
		<div class="row">
			<div class="span1 qna-que-cnt">
				<div>
					<%= question.answers.size %>
				</div>
				<div>
					<% if question.answers.size > 1 %>
						answers
					<% else %>
						answer
					<% end %>
				</div>
			</div>
			<div class="span8">
				<%= link_to content_tag(:h4, question.body), question, :class => "qna-que-link" %>
				<div class="when">
					<h6><small><%= time_ago_in_words(question.created_at) %> - by <%= mail_to question.user.email %></small></h6>
				</div>
			</div>
		</div>
	</div>
</div>
